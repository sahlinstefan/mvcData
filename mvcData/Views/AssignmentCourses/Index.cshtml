@model IEnumerable<mvcData.Models.AssignmentCourse>


<h2>Assignments / Courses</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Assignments.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Courses.Course)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Assignments.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Courses.Course)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AssignmentCourse_Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.AssignmentCourse_Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AssignmentCourse_Id })
        </td>
    </tr>
}

</table>
